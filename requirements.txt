# ===== SiliconDreams Dependencies =====
# Core Framework (Web Server)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# LLM & RAG
llama-index>=0.10.0
llama-index-embeddings-huggingface>=0.2.0
langchain>=0.2.0
langchain-community>=0.2.0

# Vector Database
chromadb>=0.4.0

# PDF Parsing
pymupdf>=1.23.0
pymupdf4llm>=0.0.6
pdfplumber>=0.10.0

# Embedding (Apple Silicon)
torch>=2.1.0
sentence-transformers>=2.2.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Financial Calculation (built-in, no extra deps)
# decimal, math — stdlib

# Web Search
# Primary: Tavily Search API (AI-agent-friendly, free tier 1,000/mo)
# Fallback: DuckDuckGo HTML (requires curl_cffi for TLS compat on macOS)
curl_cffi>=0.5  # optional — only needed for DDG fallback

# Pin urllib3 to v1.x for macOS LibreSSL compatibility
# (macOS ships LibreSSL 2.8.3; urllib3 v2 requires OpenSSL 1.1.1+)
urllib3<2

# Testing
pytest>=7.0.0
