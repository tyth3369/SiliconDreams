# Copy to .env.caddy on the server. Never commit the real file.
DOMAIN=sillycon.xyz
SITE_USERNAME=researcher
# Generate with: docker run --rm -it caddy:2-alpine caddy hash-password
SITE_PASSWORD_HASH=$2a$14$replace_with_a_real_caddy_password_hash
