# 2026-09-13

## v1.0.0-rc.1 release-candidate freeze

- Confirmed the `v1.0.0-dev.4` GitHub Actions run completed successfully after the privacy-safe operations telemetry milestone.
- Re-ran the release-only gates: two real Chromium E2E paths passed; the official annual-report benchmark remained at Recall@5 100% and MRR 84.03%; the citation benchmark remained 100% across precision, coverage and marker validity.
- Re-ran the local workbench benchmark and knowledge-graph audit. The graph contains 309 nodes and 1516 edges with no alias collisions, missing endpoints or missing inverse edges.
- Release audit found that HTMX was CDN-hosted and Marked/DOMPurify were both CDN-hosted without explicit versions. Vendored HTMX 1.9.12, Marked 15.0.12, DOMPurify 3.4.15 and the required IBM Plex WOFF2 files, retained upstream licenses and pinned reviewed SHA-256 values in tests.
- Tightened CSP to self-hosted scripts, styles and fonts only, removing runtime CDN and Google Fonts availability/privacy dependencies.
- Added `docs/release-checklist-v1.0.md`, a container-build GitHub Actions job, and frozen new feature work for the release-candidate period.
- Docker is not installed on the development Mac; the new GitHub Actions container job is the authoritative image-build and Compose-validation gate before creating the RC prerelease.

