# 2026-09-14

## v1.0.0-rc.2 local release gate

- Completed the dependency-security follow-up started during the rc.1 audit.
- Locked `pip-audit` as a development tool and exported production requirements from `uv.lock` without changing the production dependency set.
- Added a fail-closed CI production dependency audit. Four reviewed ChromaDB advisory IDs are temporarily ignored because no fixed release exists and their HTTP Server attack paths are absent from the embedded deployment.
- Added explicit security-advisory documentation and regression tests that prohibit Chroma `HttpClient`, a Chroma server/service and host publication of the application port.
- Increased the registry socket timeout to 60 seconds after reproducing transient PyPI latency; the audit still fails if the registry remains unavailable.
- Local release gates passed: 207 pytest tests at 75.71% coverage, two Chromium E2E tests, citation precision/coverage/marker validity 100%, annual-report Recall@5 100% and MRR 84.03%, and a clean 309-node/1516-edge knowledge-graph audit.
- Local dependency audit result: no unhandled vulnerabilities; five records covered by the four documented ChromaDB exceptions.
- GitHub Actions run `34771845925` passed both jobs. The quality job completed the dependency audit, lint, formatting, 207-test suite and citation benchmark; the container job validated Compose and built the production Linux x86_64 image.

## v1.0.0-rc.3 deployable release artifact

- Added a tag-triggered GHCR workflow that publishes the exact release tag, smoke-tests the pushed digest through the real `/healthz` endpoint and generates a GitHub artifact provenance attestation.
- Added `compose.registry.yaml` so an ECS deployment can pull a fixed, prebuilt image while keeping the existing source-build workflow as a fallback.
- Updated the deployment guide with public/private GHCR package handling, immutable version selection, model initialization and image-based upgrades.
- Extended CI and architecture-policy tests to validate the registry Compose override and preserve the no-host-port boundary.
- GitHub Actions run `34775169826` published the public amd64 image, re-pulled its registry digest, passed the live `/healthz` smoke test and uploaded signed provenance to the registry and Rekor. Published digest: `sha256:d7cc625f0f5ded5367bc17002c289d5e9da2685444d66cb0ef1b708af164d2a3`.
- The first metadata run revealed that metadata-action's `latest=auto` flavor adds `latest` even when the explicit raw rule is disabled for a prerelease. Set `flavor: latest=false`; future prereleases publish only their version tag, while a stable tag may explicitly update `latest`.

## v1.0.0-rc.4 deployment acceptance automation

- Added a credential-free public deployment verifier for canonical/`www` DNS, TLS, HTTP-to-HTTPS and alias redirection, exact application version, authentication boundary, security headers and production CSRF cookie attributes.
- Added an optional expected-ECS-IP assertion and reject non-public/Fake-IP DNS answers. A local check exposed `198.18.0.32` from the active proxy while public resolvers returned no A record, correctly identifying DNS as the current deployment blocker rather than treating the synthetic answer as success.
- Added human-readable and JSON output with non-zero failure status, plus unit tests for success, invalid configuration, network failure, security-policy failure and Fake-IP rejection.
- Advanced the candidate version to rc.4 and documented the exact deployment verification command. Stable v1.0 remains blocked on target-ECS manual acceptance and 24-hour observation.
- Local rc.4 gates passed: Ruff, 216 pytest tests at 75.92% coverage, two Chromium E2E tests, citation precision/coverage/marker validity 100%, official-report Recall@5 100% and MRR 84.03%, clean 309-node/1516-edge graph audit, and dependency audit with only the registered Chroma exceptions.
- Local workbench medians remained sub-millisecond (`/healthz` 0.22ms, analytics 0.31ms, watchlist 0.39ms). Docker is not installed on this Mac, so the Linux image and Compose validation remain delegated to the mandatory GitHub Actions container gate.
- Main CI run `34809586277` and tag CI run `34810709805` passed. Release-image run `34810709773` published only `v1.0.0-rc.4` (no prerelease `latest`), re-pulled digest `sha256:63a660e97d87d5784e1e85c9ff02a459e3750ea04acb0651996ce7b6243cf465`, returned `1.0.0-rc.4` from the live container and created attestation `47261142`.
- Published GitHub prerelease `v1.0.0-rc.4`. Public production promotion remains blocked only by DNS/ECS acceptance and the required observation window.

## Linux image dependency optimization

- Traced the 3.26GB release-image dependency layer to PyPI's Linux PyTorch build pulling CUDA 13 plus eighteen CUDA/NVIDIA/Triton packages on a CPU-only ECS target.
- Added an explicit, Linux-only PyTorch official CPU index while preserving the native PyPI/MPS wheel on macOS. The universal lock dropped all CUDA/NVIDIA/Triton packages and now resolves `torch+cpu` on Linux.
- Exported the full production requirement set with hashes and changed CI dependency auditing to `--disable-pip`, so the audit evaluates the locked set without re-resolving `torch+cpu` from an incorrect index.
- Added architecture-policy tests that fail if Linux loses the CPU source, hashes disappear, GPU runtime packages return, or CI resumes dependency re-resolution.
- Advanced the candidate to `v1.0.0-rc.5`; CI and the tag publisher now execute a real Linux container assertion that `torch.version.cuda is None` and report the resulting image size.
- Local rc.5 gates passed: Ruff, 217 pytest tests at 75.94% coverage, two Chromium E2E tests, dependency audit with only the registered Chroma exceptions, citation precision/coverage/marker validity 100%, official-report Recall@5 100% and MRR 84.03%, and a clean 309-node/1516-edge knowledge graph.
