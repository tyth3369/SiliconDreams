# Dev Log — 2026-09-12

## v0.8.0 official retrieval benchmark and release candidate

- Replaced the synthetic retrieval fixture with a manually reviewed 12-case bilingual golden set based on the official TSMC and SMIC 2025 annual reports.
- Locked both source PDFs by official URL, page count and SHA-256; visually checked each expected page and normalized PDF line breaks in content matching.
- Added an isolated reproducible benchmark runner that downloads, verifies, parses, chunks and indexes the reports without touching the user's production database.
- Fixed duplicate content-addressed IDs within one Chroma batch and normalized tuple/dict parser metadata before vector upsert.
- Made retrieval honor the requested `top_k`, scoped explicit company questions to matching indexed reports, and added separate original/bilingual lexical recall.
- Corrected financial-query RRF weighting and added deterministic metric-evidence coverage after Cross-Encoder ranking.
- Final official benchmark: 12/12 questions hit, Recall@5 100%, MRR 84.03%; enforced release thresholds are 90% and 70% respectively.
- Promoted the application version from v0.8.0-dev.6 to v0.8.0 release candidate and prepared release notes.

## Release verification

- `uv run ruff format .` and `uv run ruff check .`: passed.
- `uv run pytest --cov=src --cov-report=term-missing`: 128 tests passed; total coverage 69.00% (required 60%).
- `uv lock --check`, `uv pip check`, `node --check static/app.js` and `git diff --check`: passed.
- Re-ran the isolated official-report benchmark after formatting: Recall@5 100%, MRR 84.03%, above the enforced 90% / 70% gate.
- Browser smoke-tested v0.8.0: English-to-Chinese switching preserved the existing cited conversation; light/dark switching updated the root theme without losing messages; localized input placeholder and export controls remained present.

## v0.8.0 GitHub release

- Pushed release commit `9702eda` to `main`; the branch CI and immutable `v0.8.0` tag CI both passed.
- Published the non-draft, non-prerelease GitHub Release at `https://github.com/tyth3369/SiliconDreams/releases/tag/v0.8.0`.

## v0.9.0-dev.1 reusable company comparison

- Added a machine-readable financial metric registry covering definitions, units, direct comparability and required calculator operations.
- Added validated bilingual company-comparison templates shared by quick actions and future report/chart features.
- Replaced the generic peer-comparison shortcut with a concrete TSMC-versus-SMIC 2026 Q2 template.
- The template explicitly restricts revenue and gross margin to directly comparable disclosures and requires SMIC operating margin to be labelled as derived and calculated by the canonical Decimal tool.
- Added regression tests for registry scope, request validation, bilingual rendering and unknown metric rejection.
- v0.9.0-dev.1 verification: Ruff, lockfile, dependency and JavaScript checks passed; 133 pytest tests passed with 69.37% coverage.

## v0.9.0-dev.2 evidence-backed analytics

- Added a bilingual financial analytics workbench with quarterly revenue and gross-margin time series, FY2025 process-mix bars and reported Capex-intensity bars.
- Every quarterly chart point retains its exact official source title, publication date and clickable URL; SMIC process mix is explicitly labelled approximate.
- Kept all financial ratios out of the presentation layer: the dashboard only visualizes values already present in the curated official-data store.
- Added a local SVG favicon, responsive Bloomberg-style chart components and HTMX language refresh without disturbing the active conversation.
- Corrected the SMIC FY2025 annual-report provenance to the manually verified HKEX filing used by the official retrieval benchmark.
- Browser QA passed in both light and dark themes: line charts no longer fill their polygons, dashboard close works, the active conversation remains visible and the responsive two-column layout preserves the Bloomberg visual system.
- v0.9.0-dev.2 verification: 136 tests passed, coverage reached 69.77%, and Ruff, uv lock, dependency, JavaScript and diff checks all passed.

## v0.9.0-dev.3 persistent watchlist and event timeline

- Added schema v3 with an idempotent persistent company watchlist and migration coverage from legacy databases.
- Added a bilingual Watchlist workbench that follows/unfollows TSMC and SMIC without leaving the page.
- Built the event timeline deterministically from exact official quarterly disclosures, sorted by publication date with direct filing links and no LLM-generated event claims.
- Added route, storage and timeline regression coverage, including unsupported-company rejection.
- Browser QA passed for empty state, follow/unfollow persistence, responsive timeline layout and direct official-filing links; the temporary QA watch was removed afterward.
- v0.9.0-dev.3 verification: 139 tests passed, coverage reached 70.09%, and Ruff, uv lock, dependency, JavaScript and diff checks all passed.

## v0.9.0-dev.4 web evidence cache and snapshots

- Added schema v4 tables for expiring structured web-search cache entries and immutable content-addressed result snapshots.
- General searches cache for 24 hours; time-sensitive searches cache for 15 minutes and are re-assessed against the current evidence-date policy on every cache hit.
- Explicit company filing/result queries now use Tavily official-domain constraints, while broader latest-news questions retain cross-source search and trust-tier ranking.
- Added tests proving cache hits avoid duplicate backend calls, expiry is respected, snapshots deduplicate by URL/content hash and deceptive domains are not promoted.
- v0.9.0-dev.4 verification: 142 tests passed, coverage reached 70.64%, and Ruff, uv lock, dependency, JavaScript and diff checks all passed.

## v0.9.0-dev.5 browser regression and performance baseline

- Added a real Chromium E2E test with an isolated SQLite database, covering light theme, live language switching, financial charts, persistent Watchlist and preservation of the active chat DOM.
- Added Playwright to the locked dev toolchain and CI, including reproducible Chromium installation.
- Added a deterministic local route benchmark. Twenty-sample baseline on the development Mac: `/healthz` P95 0.34ms, `/analytics` P95 0.38ms and `/watchlist` P95 0.54ms.
- Added `SILICONDREAMS_DATABASE_FILE` override so subprocess tests and deployments can isolate runtime state without moving curated data.
- Local browser test passed in headless Chromium; the remaining full suite and CI are run before push.
- v0.9.0-dev.5 verification: all 143 tests including the real-browser test passed, coverage remained 70.64%, and Ruff format/lint, uv lock/export, dependency, JavaScript and diff checks passed.

## v0.9.0 terminology graph and release candidate

- Added stable term/company/concept entities, bilingual alias resolution and typed `related_to` / `associated_with` edges.
- Normalized common duplicate company labels through a reviewed alias registry and completed every unresolved relation with an explicit concept node.
- Integrated normalized graph neighbors into terminology context while preserving the original definitions and business-impact text.
- Added a release-blocking graph audit. Current result: 309 nodes, 1516 edges, zero missing endpoints, zero missing inverse edges and zero alias collisions.
- Completed every v0.9 analysis-product roadmap item and prepared the v0.9.0 release notes.

## v0.9.0 final verification

- `uv run ruff format .`, `uv run ruff check .`, `node --check static/app.js` and `git diff --check`: passed.
- `uv lock`, runtime requirements export and `uv pip check`: passed; all 127 installed packages are compatible.
- `uv run pytest --cov=src --cov-report=term-missing`: all 147 tests passed, including Chromium E2E; total coverage reached 71.57% against the 60% gate.
- Re-ran the terminology graph audit: 309 nodes, 1516 edges, no dangling endpoints, missing inverse edges or alias collisions.
- Thirty-sample route benchmark: `/healthz` P95 0.31ms, `/analytics?lang=en` P95 0.40ms and `/watchlist?lang=en` P95 0.51ms.

## v1.0.0-dev.1 transactional schema migration

- Replaced startup-time execution of one final-state schema with ordered, transactional v1→v4 migrations.
- Added immutable migration names and SHA-256 checksums, baseline adoption for pre-history v4 databases and strict rejection of future versions or inconsistent history.
- Added startup validation for required tables, columns, indexes, FTS and triggers so a version number cannot conceal partial schema drift.
- Added database administration helpers and CLI commands for read-only status, integrity verification, SQLite online backup and explicit atomic restore.
- Added regression coverage for fresh migration history, lossless legacy adoption, future-version refusal, checksum drift, structural drift, backup/restore preservation and overwrite safety.
- v1.0.0-dev.1 verification: 160 tests including Chromium E2E passed with 72.41% coverage; Ruff, lock/export, dependency, JavaScript and diff checks passed. Docker CLI was unavailable on the development Mac, so Compose validation remains a CI/deployment-host check.
- Adopted the existing local schema-v4 database as a checksummed baseline without rebuilding business tables; integrity and foreign-key checks passed. Created and re-verified the ignored online backup `backups/pre-v1.0-dev1-2026-09-12.db`.
- CI exposed stdout contamination in the JSON CLI when `.env` is absent. Moved the configuration notice to stderr and added subprocess-level CLI coverage; also isolated every pytest import from the production database before test collection.

## v1.0.0-dev.2 public-deployment security baseline

- Added first-party single-account authentication with salted PBKDF2-SHA256 password verification, HMAC-signed expiring sessions and a generator for production-safe environment values.
- Added session-bound CSRF protection for every authenticated mutation, HTMX request-header injection and protected login/logout flows without inline event handlers.
- Added independent sliding-window limits for login, AI and general requests. Client addresses are only used transiently and are persisted as keyed, truncated hashes.
- Added schema v5 append-only `audit_events`, including database triggers that reject update/delete, HTTP mutation auditing and a JSON `manage_database.py audit` command.
- Replaced Caddy Basic Auth with application authentication. `APP_ENV=production` now fails startup if authentication or Secure cookies are missing; deployment and threat-model documentation now covers proxy trust, rotation, backup and incident response.
- Tightened browser policy to nonce-based CSP without `unsafe-inline`, added HSTS for Secure-cookie deployments and retained nosniff, frame denial, referrer, permissions and opener policies.
- Updated UI fragments to native progress elements and data attributes so strict CSP remains compatible; added bilingual login/logout labels.
- Created and verified the ignored online backup `backups/pre-v1.0-dev2-2026-09-12.db`, then migrated the live local database from schema v4 to v5 without rebuilding prior data. Integrity and foreign-key checks passed.
- Verification: Ruff format/lint, lockfile, dependency, JavaScript and diff checks passed; all 179 pytest tests passed with 73.17% coverage. Real Chromium regression passed both the existing research workbench and the authenticated login/logout path.

## v1.0.0-dev.3 grounded citation quality gate

- Added a reviewed, frozen citation benchmark captured from six real bounded-Agent answers across Chinese and English company-financial and terminology questions.
- Added claim-to-source citation precision, required-claim coverage and inline-marker validity metrics. CI now enforces minimum gates of 95%, 90% and 100% respectively; the current fixture scores 100% on all three.
- Enriched terminology and structured-financial citations with bounded evidence excerpts, English names, exact reporting periods, official titles, publication metadata and direct URLs.
- Fixed explicit `FY2025` requests being misrouted as missing quarterly data, and added an integration test proving the company-data tool preserves its official evidence inside the final citation object.
- Real DeepSeek capture exposed provider-specific DSML text appearing when no valid function call was parsed. Added deterministic local company/terminology retrieval fallback so obvious evidence queries cannot silently bypass grounded tools.
- Tightened evaluator accounting so optional claims do not dilute required-claim coverage and per-case supported-marker counts cannot exceed the number of answer markers.

## v1.0.0-dev.4 privacy-safe operations observability

- Added transactional schema v6 tables for request-level AI runs and per-tool timings. The records intentionally exclude prompts, answers, source names, URLs and evidence excerpts.
- Instrumented retrieval planning, optional calculation planning, final streaming, first-token latency, model errors, tool errors and source-type coverage across the real Agent path.
- DeepSeek streaming now requests official API usage metadata; prompt, completion, cache-hit and cache-miss tokens are accumulated across all model calls.
- Added optional environment-based per-million-token prices. Cost remains `null` unless all rates are explicitly configured, and every estimate uses the canonical Decimal calculator.
- Added authenticated `/ops/metrics` and `manage_database.py metrics --hours N` views with success/error rate, average/P95 latency, token totals, source coverage and per-tool performance.
- Created and integrity-checked `backups/pre-v1.0-dev4-2026-09-12.db`, then migrated the live local database from schema v5 to v6 without losing prior records.
- Final quality gate: Ruff format/lint, lock verification, 199 tests at 75.71% coverage, citation benchmark (100% precision/coverage/marker validity), dependency compatibility, JavaScript syntax, diff whitespace, and live schema v6 integrity all passed.
- Review hardening separated degraded/cancelled rates from true errors and made malformed provider usage metadata non-fatal, so observability cannot interrupt an otherwise valid answer.
