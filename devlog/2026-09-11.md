# Dev Log — 2026-09-11

## Project takeover audit (Codex)

- Performed a read-only audit of the current FastAPI/HTMX UI, Agent loop, RAG pipeline, citations, financial data, dependencies, tests, and project documentation.
- Confirmed the current application is a useful proof of concept, but the documentation overstates several implemented capabilities.
- Highest-priority findings:
  - no Git repository and no executable tests (`pytest` collects 0 tests);
  - Python 3.9 is end-of-life and the installed environment fails `pip check` because of `grpcio` platform compatibility;
  - active chat path uses a duplicate calculator implementation instead of `src/tools/calculator.py`;
  - PDF sources are indexed under temporary filenames and page numbers are estimated instead of exact;
  - claimed BM25 / HyDE / Cross-encoder retrieval is not implemented;
  - citations enumerate retrieved sources, not claim-level supporting evidence;
  - global in-memory chat state is not durable or session-isolated;
  - unsanitized Markdown and manually assembled citation HTML create an XSS risk;
  - legacy LangChain/LlamaIndex code and dependencies remain despite being outside the active path;
  - terminology contains 100 entries, 102 dangling related-term edges, and known text defects.
- Recommended direction: retain the existing UI, rebuild the backend around typed evidence records, deterministic planning, exact-page ingestion, real hybrid retrieval, one canonical calculator, SQLite persistence, and evaluation gates.

## Next step

- Review the proposed v0.7 → v1.0 restructuring roadmap with the user before making implementation changes.

## v0.7.0 engineering foundation

- User approved the recommended `1A / 2A / 3A / 4A` direction: provider abstraction with DeepSeek V4, official-source-first financial evidence, aggressive cleanup after a recoverable baseline, and no UI redesign during backend work.
- Tightened `.env` and `.github_token` permissions to `0600`; verified both are ignored by Git.
- Initialized a local Git repository, committed the pre-refactor baseline as `23c8a57`, and tagged it `pre-codex-refactor`. No remote was added and nothing was pushed.
- Installed uv and CPython 3.12.14 (OpenSSL 3.5.8), created `.venv`, and generated `uv.lock`.
- Added `pyproject.toml`, `.python-version`, `.env.example`, Ruff, pytest/coverage, and GitHub Actions CI.
- Removed unused LangChain/ReAct files and dependencies plus the LlamaIndex embedding wrapper. BGE-M3 now loads directly through `sentence-transformers`.
- Consolidated all Agent arithmetic into `src/tools/calculator.py` with validated structured arguments. Removed the conflicting string-expression calculator implementation.
- Added 56 passing tests covering calculator semantics, terminology, company lookup, citation safety, SQLite persistence, session isolation, and HTTP smoke behavior.
- Added `src/storage.py` with SQLite models for sources, documents, chunks + FTS5, facts, conversations, messages, and jobs.
- Replaced process-global chat history with cookie-isolated SQLite conversations; reloads and server restarts can retain messages and citations.
- Added official filing metadata for TSMC and SMIC, normalized monetary units to USD billions, corrected TSMC FY2025 revenue/net-margin values, and synchronized 16 normalized facts into SQLite.
- Added DOMPurify to Markdown rendering, scheme validation and escaping to citation HTML, and message-local citation targeting to prevent duplicate anchors.

## Verification

- `uv pip check`: all installed packages compatible.
- `uv run ruff check config.py server.py src tests`: passed.
- `uv run pytest`: 56 passed.

## Next implementation slice

- Preserve original PDF filenames and hashes, use exact page metadata from PyMuPDF4LLM, write document/chunk provenance to SQLite, and make Chroma ingestion idempotent.

## v0.7.0 PDF evidence and hybrid retrieval slice

- Reworked PDF parsing to consume PyMuPDF4LLM page chunks, preserving exact 1-indexed page provenance instead of estimating pages from character positions.
- Uploads now validate the real byte length and PDF magic signature, sanitize filenames, use SHA-256 content-addressed storage, and persist document ingestion status/errors in SQLite.
- Chunk IDs are deterministic; SQLite chunk IDs are reused as Chroma vector IDs so lexical and dense results refer to the same evidence record.
- Changed Chroma writes to idempotent upserts and configured cosine distance. Empty collections no longer initialize the 2.3 GB embedding model.
- Replaced the previous keyword-bonus pseudo-hybrid retrieval with actual BM25 scoring over persisted chunks, Chinese character n-gram tokenization, and weighted reciprocal-rank fusion with BGE-M3 dense retrieval.
- Added parser, exact-page provenance, deterministic ID, vector idempotency, Chinese BM25, RRF, PDF upload, and spoofed-file regression tests.

## Verification (PDF/RAG slice)

- `uv run ruff check config.py server.py src tests`: passed.
- `uv run pytest`: 67 passed.

## Next implementation slice

- Add a model-backed multilingual cross-encoder reranker and retrieval quality evaluation fixtures, then replace the open-ended Agent loop with a bounded typed planner.

## v0.7.0 deterministic evidence architecture

- Added a local multilingual Cross-Encoder reranker and a resumable downloader; verified the cached model with real inference.
- Added Recall@K/MRR evaluation code, a CLI, and clearly labelled synthetic fixtures. Real quality claims remain gated on a human-reviewed PDF golden set.
- Introduced a minimal Provider interface and a DeepSeek V4 implementation. The legacy client is now only a compatibility facade.
- Replaced the open-ended Agent loop with one bounded retrieval-planning call, concurrent retrieval execution, at most one calculation-planning call, and a final streaming generation pass.
- Added typed Pydantic tool arguments, exact duplicate suppression, hard per-tool budgets, and dynamic tool availability so unavailable PDF/Web paths are never advertised.
- Kept all arithmetic in the canonical Decimal calculator; no duplicate expression evaluator remains.
- Added official/reputable/general Web-source trust tiers and carried publisher/date/trust metadata through citations and the final answer source index.
- Hardened prompts against prompt injection and unsupported factual completion; the current date is injected server-side and the model context is bounded to the latest 24 messages.
- Added security headers, 4,000-character input limits, bounded/expiring SSE hand-off state, and worker-thread offload for synchronous Agent and PDF ingestion work.
- Removed unsourced analyst strengths/risks from the financial context and bundled data; structured annual metrics retain exact official report metadata.
- Removed emoji from active application/downloader logs and synchronized stale version/model comments.

## UI and browser regression

- Preserved the existing Bloomberg Terminal visual design.
- Verified in a real browser that light mode uses white text on the active blue toggle.
- Verified Chinese/English switching without page reload or conversation loss.
- Fixed sidebar/upload/source-panel translation refreshes, removed a nested duplicate sidebar wrapper, and confirmed message-local inline citation links remain intact.
- Kept Markdown sanitization, rich tables, source expansion, and persistent SQLite-backed chat behavior.

## Documentation and environment cleanup

- Rewrote README, AGENTS.md, requirements, technical spec, design spec, execution plan, API guide, and dependency guide to match the active v0.7 implementation.
- Moved the obsolete 1.3 GB `venv/` directory to the recoverable Trash location `/Users/eric/.Trash/SiliconDreams-venv-old`; `.venv/` is the only active environment.
- Updated Tavily placeholder detection so a copied `.env.example` cannot be mistaken for a configured API key.

## Final verification

- `uv run ruff format --check .`: passed.
- `uv run ruff check .`: passed.
- Final regression suite: 88 tests passed; the measured coverage gate remained above the required 60%.
- `uv lock --check`: passed.
- `uv pip check`: all installed packages compatible.
- Browser smoke: layout, language/theme switch, persistent conversation, inline citation and source panel passed.
- A supplemental remote `pip-audit` attempt was stopped after the PyPI advisory service did not respond within the bounded wait; this did not affect lock or installed-package consistency checks.
